<%= form_with(model: program, local: true) do |form| %>
  <% if program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(program.errors.count, "error") %> prohibited this program from being saved:</h2>

      <ul>
      <% program.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :program_user_id %>
  </div>

  <div class="field">
    <%= form.label :show_id %>
    <%= form.number_field :show_id, id: :program_show_id %>
  </div>

  <div class="field">
    <%= form.label :favorite %>
    <%= form.check_box :favorite, id: :program_favorite %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
